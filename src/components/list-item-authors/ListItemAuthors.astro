---
type Props = {
  values: string[];
};

const listFormatter = new Intl.ListFormat("en", {
  style: "long",
  type: "conjunction",
});

const { values } = Astro.props;
---

<div class={`text-[15px]/4 font-normal tracking-prose text-muted`}>
  {listFormatter.format(values)}
</div>
