## 2026-02-24 - Stage 1: Foundation — New Shared Components

- What was implemented:
  - Created `src/components/filter-and-sort/FilterSection.tsx` — direct port from movie log. Collapsible details/summary with smooth height+opacity animations via direct DOM manipulation.
  - Created `src/components/filter-and-sort/FilterSection.spec.tsx` — direct port. 10 tests covering open/closed defaults, title, disclosure triangle, initial height, snapshots.
  - Created `src/components/filter-and-sort/AppliedFilters.tsx` — direct port. Renders removable filter chips wrapped in FilterSection. Adapted sample data in comments for book log domain (Kind/Novel vs Genre/Horror).
  - Created `src/components/filter-and-sort/AppliedFilters.spec.tsx` — adapted port. Same structure as movie log but uses book log categories (Kind, Search). 17 tests.
  - Created `src/components/fields/CheckboxListField.tsx` — direct port. Multi-select with selected-first sorting, show-more, clear, form reset listener, defaultValues sync via useEffect.
  - Created `src/components/fields/RangeSliderField.tsx` — direct port. Dual-handle range slider with formatValue, keyboard nav, ARIA attributes, screen-reader live region.
  - Created `src/components/fields/RangeSliderField.spec.tsx` — direct port. 20 tests.
  - Created `src/utils/grades.ts` — new file. Exports `gradeToLetter(number → string)` and `gradeToValue(string → number)` using the 2–16 scale (F-=2, A+=16). Consolidates two private copies that existed in api/reviews.ts and getAuthorTitlesProps.ts.

- Files changed:
  - NEW: src/components/filter-and-sort/FilterSection.tsx
  - NEW: src/components/filter-and-sort/FilterSection.spec.tsx
  - NEW: src/components/filter-and-sort/AppliedFilters.tsx
  - NEW: src/components/filter-and-sort/AppliedFilters.spec.tsx
  - NEW: src/components/fields/CheckboxListField.tsx
  - NEW: src/components/fields/RangeSliderField.tsx
  - NEW: src/components/fields/RangeSliderField.spec.tsx
  - NEW: src/utils/grades.ts

- Test results: 253 tests passed, 5 snapshots written (0 failures)

- **Learnings for future iterations:**
  - The movie log's `CheckboxListField` imports `LabelText` from `./LabelText` — the book log already has `src/components/fields/LabelText.tsx`, so no extra file needed.
  - `AppliedFilters.spec.tsx` was adapted (not pure port) to use book log categories (Kind/Novel, Search/title) instead of movie log categories (Genre/Horror) — keeps tests meaningful for this domain.
  - `src/utils/grades.ts` already existed in the movie log at the same path; the book log didn't have it yet.
  - The movie log's `grades.ts` only exported `gradeToLetter`; the spec requires also exporting `gradeToValue` — both are included.
  - All 8 Stage 1 tasks completed in one pass; no regressions in existing tests.

---

## 2026-02-24 - Stage 2: Field Updates + FilterAndSortContainer/Header

- What was implemented:
  - Modified `src/components/fields/GradeField.tsx` — extended grade scale 1–13 → 2–16 (F-=2, A+=16). Added `FilterSection` wrapper, `RangeSliderField`, `onClear` prop, `useEffect` state sync. `handleClear` resets to `[2, 16]`.
  - Modified `src/components/fields/YearField.tsx` — added `FilterSection` wrapper, `RangeSliderField`, `onClear` prop, `useEffect` state sync, `findClosestYear` function for sparse year arrays. Slider syncs bidirectionally with dropdowns via numeric year values.
  - Modified `src/components/filter-and-sort/FilterAndSortContainer.tsx` — complete rewrite. New `SortOption = { label, value }` type; `SortProps<T>` now uses `sortOptions: readonly SortOption[]` and `onSortChange: (value: T) => void`. Added `sideNav`, `activeFilters`, `onRemoveFilter` props. Added `AppliedFilters` section in drawer, mobile sort radio section (uncontrolled, read via `FormData` on "View Results"), `suppressSortScrollRef` to prevent auto-scroll when sort changes via mobile drawer. Renamed `topNav` → `sideNav` (layout now `flex flex-row-reverse`).
  - Modified `src/components/filter-and-sort/FilterAndSortHeader.tsx` — sort dropdown hidden on mobile (`hidden tablet:block`). Filter button shows "Filter & Sort" on mobile, "Filter" on desktop. Options rendered via `sortOptions.map()`.
  - Modified `src/components/filter-and-sort/ReviewedWorkSortOptions.tsx` — converted from JSX component to exported `REVIEWED_WORK_SORT_OPTIONS: readonly SortOption[]` constant.
  - Modified `src/components/filter-and-sort/CollectionSortOptions.tsx` — same conversion to `COLLECTION_SORT_OPTIONS: readonly SortOption[]`.
  - Modified `src/components/filter-and-sort/FilterAndSortContainer.spec.tsx` — added "mobile sort section" describe block (7 tests) and "applied filters" describe block (6 tests). Updated `sortOptions` mock from JSX to array.
  - Modified `src/components/filter-and-sort/FilterAndSortContainer.testHelper.ts` — added `clickSortRadioOption(user, value)`.
  - Modified `src/features/authors/Authors.tsx` — `topNav` → `sideNav`, `COLLECTION_SORT_OPTIONS`, `onSortChange(value)`.
  - Modified `src/features/reviews/Reviews.tsx` — `REVIEWED_WORK_SORT_OPTIONS` spread with author options, `onSortChange(value)`.
  - Modified `src/features/author-titles/AuthorTitles.tsx` — `REVIEWED_WORK_SORT_OPTIONS`, `onSortChange(value)`.
  - Modified `src/features/reading-log/ReadingLog.tsx` — inline sort options as array, `onSortChange(value)`.
  - Fixed `src/features/reviews/Reviews.spec.tsx` and `AuthorTitles.spec.tsx` — updated grade filter test data to new scale (F→3, B→12, A+→16).

- Files changed:
  - MODIFIED: src/components/fields/GradeField.tsx
  - MODIFIED: src/components/fields/YearField.tsx
  - MODIFIED: src/components/filter-and-sort/FilterAndSortContainer.tsx
  - MODIFIED: src/components/filter-and-sort/FilterAndSortHeader.tsx
  - MODIFIED: src/components/filter-and-sort/ReviewedWorkSortOptions.tsx
  - MODIFIED: src/components/filter-and-sort/CollectionSortOptions.tsx
  - MODIFIED: src/components/filter-and-sort/FilterAndSortContainer.spec.tsx
  - MODIFIED: src/components/filter-and-sort/FilterAndSortContainer.testHelper.ts
  - MODIFIED: src/features/authors/Authors.tsx
  - MODIFIED: src/features/reviews/Reviews.tsx
  - MODIFIED: src/features/author-titles/AuthorTitles.tsx
  - MODIFIED: src/features/reading-log/ReadingLog.tsx
  - MODIFIED: src/features/reviews/Reviews.spec.tsx (grade scale fix)
  - MODIFIED: src/features/author-titles/AuthorTitles.spec.tsx (grade scale fix)

- Test results: 266 tests passed, snapshots updated (0 failures). `npm run check` clean (0 errors).

- **Learnings for future iterations:**
  - Grade scale migration breaks grade filter tests: test mock data must use new scale values (F=3, B=12, A+=16). The filter compares `gradeValue` numerically against the slider/dropdown range [min, max].
  - `suppressSortScrollRef` pattern: mobile drawer "View Results" reads sort from `FormData`, calls `onSortChange`, sets `suppressSortScrollRef.current = true` BEFORE `onSortChange` so the sort-change `useEffect` skips the scroll (the drawer closes and scrolls explicitly instead).
  - `topNav` → `sideNav` is a layout change: sideNav renders inside `flex flex-row-reverse` alongside the list, not above it. Authors' `AlphabetNav` moved from above-list to alongside-list this way.
  - `SortProps<T>` cascade was large: 2 sort option files + 4 feature components + header + spec all needed updating when changing from `React.ReactNode` to `readonly SortOption[]`.
  - Stage 2 did NOT include TextField.tsx changes (not required by plan tasks).
  - **NOT YET COMMITTED** — needs lint/format pass before commit.

- **Next: Stage 3** — Reducer layer changes:
  - `filtersReducer.ts`: add `RemoveAppliedFilterAction`.
  - `titleFiltersReducer.ts`: `kind` string → `readonly string[]`.
  - `reviewedTitleFiltersReducer.ts`: `reviewedStatus` string → `readonly string[]`, add `gradeValue` remove handler.
  - `collectionFiltersReducer.ts`: add `name` remove handler.
  - `ReadingLog.reducer.ts`: `edition`/`reviewedStatus` string → `readonly string[]`.
  - `filterTitles.ts`, `filterReadingLog.ts`, `createReviewedStatusFilter.ts`, `filterReviewedTitles.ts`: type updates.
  - `api/reviews.ts`, `getAuthorTitlesProps.ts`: replace private `gradeToValue` with `~/utils/grades`.

---

## 2026-02-24 - Stage 2: Lint/Format Pass + Commit

- What was implemented:
  - Fixed ESLint errors: removed unused `ReviewsSort` import from `Reviews.tsx`.
  - Added `--background-color-stripe` CSS variable to `src/css/tailwind.css` (used by `CheckboxListField` and `RangeSliderField` for the `bg-stripe` Tailwind utility class).
  - Added `.range-input` component styles to `src/css/tailwind.css` (used by `RangeSliderField` for styling dual-handle range slider thumbs across webkit and moz).
  - Fixed `perfectionist/sort-objects` and `better-tailwindcss/enforce-consistent-class-order` lint errors via `npm run lint:fix`.
  - Ran Prettier on 5 files (`FILTER_REDESIGN_SPEC.md`, 4 feature tsx files).
  - All 266 tests pass. `npm run lint`, `npm run check`, `npm run format` all clean.

- Files changed:
  - MODIFIED: src/features/reviews/Reviews.tsx (removed unused import)
  - MODIFIED: src/css/tailwind.css (added bg-stripe CSS var + range-input component)
  - MODIFIED: src/components/fields/RangeSliderField.tsx (class order fix by lint:fix)
  - MODIFIED: src/utils/grades.ts (object key sort fix by lint:fix)
  - MODIFIED: src/features/author-titles/AuthorTitles.tsx (prettier)
  - MODIFIED: src/features/authors/Authors.tsx (prettier)
  - MODIFIED: src/features/reading-log/ReadingLog.tsx (prettier)
  - MODIFIED: FILTER_REDESIGN_SPEC.md (prettier)

- **Learnings for future iterations:**
  - Porting `CheckboxListField` and `RangeSliderField` from movie log requires adding their CSS dependencies: `--background-color-stripe` CSS variable and `.range-input` component styles to `tailwind.css`. The book log linter (`better-tailwindcss/no-unknown-classes`) catches missing classes immediately.
  - `perfectionist/sort-objects` requires numeric object keys to be sorted ascending (e.g., `{2: ..., 3: ..., 4: ...}`) — the grade maps from the spec have them sorted descending for readability; linter fixes them automatically.
  - When removing a type import, also check that the type wasn't actually needed for any prop/type annotations elsewhere in the file.

---

## 2026-02-24 - Stage 3: Reducer Layer (IN PROGRESS — context limit hit)

- What was implemented so far:
  - `src/reducers/filtersReducer.ts`: Added `RemoveAppliedFilterAction`, `createRemoveAppliedFilterAction`, base `removeAppliedFilter` handler (deletes whole key from both active+pending). AIDEV-NOTE added about child reducer overrides.
  - `src/reducers/titleFiltersReducer.ts`: Changed `kind?: string` → `kind?: readonly string[]`. Updated `KindFilterChangedAction` (value→values). Added `removeAppliedFilter` override for `kind-*`. Re-exported `createRemoveAppliedFilterAction`.
  - `src/reducers/reviewedTitleFiltersReducer.ts`: Added `reviewedStatus?: readonly string[]`. Added `ReviewedStatusFilterChangedAction`, `createReviewedStatusFilterChangedAction`. Added `removeAppliedFilter` overrides for `"gradeValue"`, `"reviewYear"`, `reviewedStatus-*`. Re-exported both new action creators.
  - `src/reducers/collectionFiltersReducer.ts`: Added `removeAppliedFilter` override for `"name"`. Re-exported `createRemoveAppliedFilterAction`.
  - `src/features/reading-log/ReadingLog.reducer.ts`: Changed `edition`/`reviewedStatus` to `readonly string[]`. Updated actions, handlers. Added `removeAppliedFilter` overrides for `edition-*` and `reviewedStatus-*`. Re-exported `createRemoveAppliedFilterAction`.
  - `src/filterers/filterTitles.ts`: `createKindFilter` → `readonly string[]`, uses `.includes()`.
  - `src/features/reading-log/filterReadingLog.ts`: `createEditionFilter`/`createKindFilter` → `readonly string[]`.
  - `src/filterers/createReviewedStatusFilter.ts`: Added `abandoned: boolean` to type. Changed `filterValue` to `readonly string[]`. Filter logic checks `value.abandoned` → "Abandoned" status.
  - `src/filterers/filterReviewedTitles.ts`: Added `abandoned: boolean` to `FilterableReviewedTitle`. Added `createReviewedStatusFilter` call. `createGradeFilter` treats `[2, 16]` as no-op (AIDEV-NOTE added).
  - `src/api/reviews.ts`: Replaced private `gradeToValue` (old 1-12 scale) with `~/utils/grades` import. Added `abandoned: boolean` to `Review` type.
  - `src/features/author-titles/getAuthorTitlesProps.ts`: Replaced private `gradeToValue` with `~/utils/grades`. Added `abandoned` to value construction.
  - `src/features/author-titles/AuthorTitles.tsx`: Added `abandoned: boolean` to `AuthorTitlesValue`.
  - `src/features/reading-log/ReadingLog.tsx`: Added `abandoned: boolean` to `ReadingLogValue`.
  - `src/features/reading-log/getReadingLogProps.ts`: Populated `abandoned: entry.progress === "Abandoned"`.
  - `src/features/reviews/Reviews.tsx`: Added `abandoned: boolean` to `ReviewsValue`.
  - `src/features/reviews/getReviewsProps.ts`: Populated `abandoned: review.abandoned`.
  - TypeScript cascade fixes (pulled forward from Stage 4):
    - `WorkFilters.tsx`: Replaced `SelectField` with `CheckboxListField` inside `<FilterSection title="Kind">`. Updated kind prop to `readonly string[]`.
    - `ReviewedStatusFilter.tsx`: Converted to `CheckboxListField` inside `<FilterSection title="Status">`. Added `excludeNotReviewed` prop.
    - `ReviewedWorkFilters.tsx`: Added `reviewedStatus` prop, `onClear` for grade/reviewYear.
    - `ReviewsFilters.tsx`, `AuthorTitlesFilters.tsx`, `ReadingLogFilters.tsx`: Updated to use array-based APIs. Edition now uses `CheckboxListField`.
    - `Reviews.reducer.ts`, `AuthorTitles.reducer.ts`: Re-exported new action creators.
    - `WorkFilters.testHelper.ts`: `clickKindFilterOption` now finds by `.value` attribute (NOT `getByRole` name — counts like "(0)" break name matching). `getKindFilter()` returns `getByRole("group", { name: "Kind" })`.
    - `ReviewedStatusFilter.testHelper.ts`: Updated to find by `.value` attribute.
    - All 3 feature spec fixtures: Added `abandoned: false` to value factories.

- **Status:** TypeScript clean. Tests FAILING — 10 failures in 3 specs.

- **Root cause of remaining test failures:**
  1. `ReadingLog.testHelper.ts` still uses `clickSelectFieldOption(user, "Edition", value)` — edition is now CheckboxListField. Fix: find by `.value` attribute like kind.
  2. `getEditionFilter()` uses `getByLabelText("Edition")` — no longer a select. Fix: `getByRole("group", { name: "Edition" })`.
  3. "Clears all filters" tests in all 3 specs call `expect(getKindFilter()).toHaveValue("All")` or `expect(getEditionFilter()).toHaveValue("All")`. These were SelectField assertions. Fix: assert that no checkboxes are checked (e.g., check all checkboxes have `checked: false`).
  4. Snapshots need regeneration after tests pass: `npm run test:update -- --max-workers=2`.

- **Learnings for future iterations:**
  - `CheckboxListField` renders count in label text: `"Novel (0)"`. `getByRole("checkbox", { name: "Novel" })` FAILS — always use `.value` attribute lookup for checkboxes in test helpers.
  - Stage 3 and Stage 4 are tightly coupled — the TypeScript cascade from string→array type changes forces filter component updates before `npm run check` passes.
  - Old SelectField "clear" assertions (`toHaveValue("All")`) must be replaced with checkbox-based assertions when converting to CheckboxListField.
  - The `ReadingLog.testHelper.ts` `clickEditionFilterOption` and `getEditionFilter` need updating (use same pattern as kind filter helpers).

---

## 2026-02-24 - Stage 3: Test Fixes + Commit

- What was implemented:
  - Fixed 8 failing tests from Stage 3's CheckboxListField migration.
  - `WorkFilters.tsx`: added `.filter(v => v !== "All")` before building CheckboxListField options. The old SelectField used "All" as a sentinel value, but CheckboxListField doesn't need it — and including "All" as a checkbox option consumed one of the 3 visible slots (showMoreThreshold=3), hiding "Novel" alphabetically.
  - `ReadingLogFilters.tsx`: same fix for edition options.
  - `ReadingLog.testHelper.ts`: updated `clickEditionFilterOption` — scopes to `getEditionFilter()` fieldset, clicks "Show more" if visible (so hidden options like "Paperback" become accessible), then finds checkbox by `.value`. Updated `getEditionFilter` to `getByRole("group", { name: "Edition" })`. Removed `clickSelectFieldOption` import.
  - `Reviews.spec.tsx`, `AuthorTitles.spec.tsx`, `ReadingLog.spec.tsx`: replaced `toHaveValue("All")` assertions with `within(getKindFilter()/getEditionFilter()).queryAllByRole("checkbox", { checked: true })` length checks.
  - `filtersReducer.ts` + `reviewedTitleFiltersReducer.ts`: replaced `const { [action.id]: _active, ...rest }` destructuring (flagged as unused var by ESLint) with `Object.fromEntries(Object.entries(...).filter(([k]) => k !== action.id))`.

- Files changed:
  - MODIFIED: src/components/filter-and-sort/WorkFilters.tsx
  - MODIFIED: src/features/reading-log/ReadingLogFilters.tsx
  - MODIFIED: src/features/reading-log/ReadingLog.testHelper.ts
  - MODIFIED: src/features/reviews/Reviews.spec.tsx
  - MODIFIED: src/features/author-titles/AuthorTitles.spec.tsx
  - MODIFIED: src/features/reading-log/ReadingLog.spec.tsx
  - MODIFIED: src/reducers/filtersReducer.ts
  - MODIFIED: src/reducers/reviewedTitleFiltersReducer.ts

- Test results: 266 tests passed (0 failures). Stage 3+4 committed together.

- **Learnings for future iterations:**
  - `CheckboxListField` has `showMoreThreshold=3` default. "All" sentinel from old SelectField must be filtered out (`kind.values.filter(v => v !== "All")`) — otherwise it consumes a visible slot, hiding real options.
  - `clickEditionFilterOption` must click "Show more" first before searching for the checkbox, since items beyond threshold=3 are hidden. Scope search to `within(getEditionFilter())` to avoid finding checkboxes from other fieldsets.
  - Destructuring to omit a key creates unused vars flagged by ESLint — use `Object.fromEntries` pattern instead.

---

## 2026-02-24 - Stage 5: Applied Filters Integration + Commit

- What was implemented:
  - Created `buildAppliedFilterChips.ts` for all four features (reviews, authors, author-titles, reading-log).
  - reviews/author-titles chips: title (Search), kind[] (Kind), workYear (Work Year — omit if full range), gradeValue (Grade — omit if [2,16]), reviewYear (Review Year — omit if full range), reviewedStatus[] (Status). Grade label: `gradeToLetter(min) + " to " + gradeToLetter(max)`.
  - authors chips: name only → `{ id: "name", category: "Search", label: name }`.
  - reading-log chips: title, kind[], edition[], workYear, readingYear, reviewedStatus[].
  - Added `createRemoveAppliedFilterAction` to `Authors.reducer.ts` re-exports (was missing).
  - Updated all four feature main components: import `buildAppliedFilterChips` + `createRemoveAppliedFilterAction`, compute `activeFilters`, pass `activeFilters` + `onRemoveFilter` to FilterAndSortContainer. Changed `onClearFilters` to dispatch BOTH `createClearFiltersAction()` AND `createApplyFiltersAction()`.

- Files changed:
  - NEW: src/features/reviews/buildAppliedFilterChips.ts
  - NEW: src/features/authors/buildAppliedFilterChips.ts
  - NEW: src/features/author-titles/buildAppliedFilterChips.ts
  - NEW: src/features/reading-log/buildAppliedFilterChips.ts
  - MODIFIED: src/features/authors/Authors.reducer.ts
  - MODIFIED: src/features/reviews/Reviews.tsx
  - MODIFIED: src/features/authors/Authors.tsx
  - MODIFIED: src/features/author-titles/AuthorTitles.tsx
  - MODIFIED: src/features/reading-log/ReadingLog.tsx

- Test results: 266 tests passed (0 failures). Lint, check, format clean.

- **Learnings for future iterations:**
  - `gradeValue[0]` = min (worse grade, e.g., F-=2), `gradeValue[1]` = max (better grade, e.g., A+=16). Grade chip label: `gradeToLetter(min) + " to " + gradeToLetter(max)`.
  - `onClearFilters` must dispatch BOTH `createClearFiltersAction()` + `createApplyFiltersAction()` so the "Clear all" chips link (wired as `onClearAll={onClearFilters}`) immediately clears both active and pending. Without the apply action, chips remain until "View Results" is clicked.
  - Full-range detection for year chips: `filterValues.workYear[0] !== distinctWorkYears[0] || filterValues.workYear[1] !== distinctWorkYears.at(-1)`. If both match the available range, omit chip.
  - `norm(s)` = `s.toLowerCase().replaceAll(" ", "-")` for chip IDs.

- **Next: Stage 6** — Test Coverage
  - Reviews.spec.tsx: add kind multi-select test (OR logic), abandoned filter test, applied filter chip tests (apply filter → open drawer → chips visible; × removes immediately).
  - AuthorTitles.spec.tsx: same pattern.
  - ReadingLog.spec.tsx: edition multi-select, reviewedStatus multi-select/abandoned, applied filter chips.
  - Authors.spec.tsx: applied filter chip test for name search.
  - After tests: run `npm run knip`, `npm run lint:spelling`, full suite.
  - Stage 6 imports needed: `clickAbandonedFilterOption` from ReviewedStatusFilter.testHelper (add helper first), `clickReviewedStatusFilterOption`.

---

## 2026-02-25 - Stage 6: Test Coverage + Knip Cleanup

- What was implemented:
  - Added `clickAbandonedFilterOption` helper to `ReviewedStatusFilter.testHelper.ts` (convenience wrapper for "Abandoned" status checkbox).
  - `Reviews.spec.tsx`: Added multi-select kind OR logic test, abandoned status filter test, and "applied filters" describe block (3 tests: chip visible after applying kind filter, chip × removal restores items, search chip visible after title filter).
  - `Authors.spec.tsx`: Added "applied filters" describe block (2 tests: search chip visible after name filter, chip × removal restores all authors).
  - `AuthorTitles.spec.tsx`: Added multi-select kind OR logic test, abandoned status filter test, and "applied filters" describe block (2 tests).
  - `ReadingLog.spec.tsx`: Added multi-select edition OR logic test, multi-select kind OR logic test, abandoned status filter test, and "applied filters" describe block (2 tests: edition chip visible, edition chip × removal restores items).
  - Deleted `SelectField.tsx`, `SelectField.testHelper.ts`, `SelectOptions.tsx` — confirmed unused by knip after CheckboxListField migration.
  - Removed unused `clickSortRadioOption` export from `FilterAndSortContainer.testHelper.ts`.
  - Made `CheckboxListFieldOption` type non-exported (not used externally).
  - Removed duplicate `RemoveAppliedFilterAction` re-export from `titleFiltersReducer.ts`.
  - Deleted `FILTER_REDESIGN_SPEC.md` and `FILTER_REDESIGN_PLAN.md` (all stages complete).
  - Regenerated `project-words.txt` to include `fieldsets`.

- Files changed:
  - MODIFIED: src/components/filter-and-sort/ReviewedStatusFilter.testHelper.ts
  - MODIFIED: src/features/reviews/Reviews.spec.tsx
  - MODIFIED: src/features/authors/Authors.spec.tsx
  - MODIFIED: src/features/author-titles/AuthorTitles.spec.tsx
  - MODIFIED: src/features/reading-log/ReadingLog.spec.tsx
  - MODIFIED: src/components/fields/CheckboxListField.tsx (type unexported)
  - MODIFIED: src/reducers/titleFiltersReducer.ts (removed re-export)
  - MODIFIED: src/components/filter-and-sort/FilterAndSortContainer.testHelper.ts (removed unused export)
  - MODIFIED: project-words.txt (regenerated)
  - DELETED: FILTER_REDESIGN_SPEC.md, FILTER_REDESIGN_PLAN.md
  - DELETED: src/components/fields/SelectField.tsx, SelectField.testHelper.ts, SelectOptions.tsx

- Test results: 282 tests passed (0 failures). lint, check, format, knip, lint:spelling all clean.

- **Learnings for future iterations:**
  - `clickAbandonedFilterOption` must use `.value` attribute lookup (not `getByRole` name) because CheckboxListField always renders `({count})` after the label text, even when count=0.
  - Applied filter chips are inside the filter drawer: must call `clickToggleFilters` to open the drawer before chips are queryable via `screen.getByRole`.
  - Chip removal (`onRemove`) does NOT close the drawer — list updates immediately while drawer stays open. Test can query both the disappearing chip and the restored list item without reopening.
  - `perfectionist/sort-modules` requires function exports in alphabetical order; `clickAbandonedFilterOption` must come before `clickReviewedStatusFilterOption`.
  - CheckboxListFieldOption type: if exported but not imported externally, knip flags it — remove the `export` keyword.
  - `RemoveAppliedFilterAction` re-exports: only re-export a type from a module if consumers actually import it from that module.

---
